<div class="container mx-auto p-4 scan">
    <div>
        <h1 class="font-bold text-3xl mb-2">Add new origin configuration </h1>
        <form method="post" action="/scanwebsite">
            <p>Add new origin</p>
            <div class="flex gap-3 my-4">
                <input type="text" class="border-2">
                <select name="" id="">
                    <option value="">HTTP</option>
                    <option value="">HTTPS</option>
                </select>
                <input type="text" placeholder="port">
                <button class="bg-green-800 text-white px-4 py-2">Add entry</button>
            </div>
        </form>
    </div>
    <div>
        <table class="border w-full">
            <tr>
                <th>Domain name</th>
                <th>Protocol</th>
                <th>Port</th>
            </tr>
            <tr>

            </tr>
        </table>
    </div>

{{#if result}}
    <hr>
    <h2 class="font-bold text-2xl my-2">Scan Report</h2>
    <table class="border w-1/2">
        <tr>
            <th> Index </th>
            <th> Values </th>
        </tr>
        <tr>
            <td> Website </td>
            <td>{{result.seedUrl }}</td>
        </tr>
        <tr>
            <td> PagesVisited </td>
            <td> {{result.pagesVisited }}</td>
        </tr>
        <tr>
            <td> Total Messages </td>
            <td> {{result.totalMessages }}</td>
        </tr>
        <tr>
            <td> Outgoing Messages </td>
            <td> {{result.outgoing }}</td>
        </tr>
        <tr>
            <td> Incoming Messages </td>
            <td> {{result.incoming }}</td>
        </tr>
        <tr>
            <td> Wildcard Outgoing </td>
            <td> {{result.wildcardOutgoing }}</td>
        </tr>
        <tr>
            <td> Heuristic </td>
            <td> {{result.incomingNoOriginCheckHeuristic }}</td>
        </tr>
    </table>
{{/if}} 
</div>